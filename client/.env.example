# Socket.io + REST API base URL (no trailing slash).
# Local dev: leave unset or use http://localhost:3001
# Production (Vercel): set in Vercel → Project → Settings → Environment Variables
# to your hosted server URL, e.g. https://your-service.up.railway.app
# VITE_SERVER_URL=https://your-api.example.com
